[Unit]
Description=news-radar: fast feeds (tsunami bulletins) every 5 minutes
After=docker.service

[Service]
Type=oneshot
WorkingDirectory=%h/dev/homelab/news-radar
ExecStart=%h/dev/homelab/news-radar/.venv/bin/python ingest.py primary --only tsunami
