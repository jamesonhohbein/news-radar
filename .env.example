# Copy to .env (mode 600). Generated passwords; never reuse elsewhere.
POSTGRES_PASSWORD=change-me
# Host port for the database (loopback only).
POSTGRES_PORT=5443
# Acute thresholds. Both unset = detector logs candidates and sends nothing.
#ACUTE_Z=
#ACUTE_N=
